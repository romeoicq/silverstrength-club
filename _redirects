# Redirect old Netlify preview domain to custom domain
# DISABLED — custom domain DNS is dead. Site serves from subdomain directly.
# https://silverstrength-club.netlify.app/*  https://silverstrength.club/:splat  301!